# Copy to `.env` (gitignored) and fill in.
#
# Read by the `eval` task only — `pnpm test` runs against a mock model and needs
# no key. Turbo declares this variable on that task so it is not hashed out.
OPENAI_API_KEY=
