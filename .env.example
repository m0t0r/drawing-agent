# Copy to `.env` (gitignored) and fill in.
#
# Read by `pnpm dev` and by the `eval` task. `pnpm test` runs against a mock
# model and needs no key. See packages/agent/README.md for how it reaches the
# evals, and apps/web/next.config.ts for how it reaches the app.
OPENAI_API_KEY=
